Simple Search Bar Rails Application.

It uses Ajax to search the specified Directory for image names
and then displays them in a list below the search bar.

This was to help user princezuda on irc#rubyonrails

Hope this helps.